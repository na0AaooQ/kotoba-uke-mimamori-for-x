# AGENTS.md

このドキュメントは、本リポジトリで作業する Codex などの AI コーディングエージェント向けの開発方針です。

リポジトリ
https://github.com/na0AaooQ/kotoba-uke-mimamori-for-x

## プロジェクト概要

本リポジトリは、Google Chrome拡張機能「ことばうけみまもり｜Xことばに心のワンクッション」のソースコード管理リポジトリです。

この拡張機能は、X（旧Twitter）で届く言葉のなかで、受け手の人の心に大きな負荷を与える可能性のある投稿について、読む前のワンクッションを置くことを目的とします。

思想や信条、異なる意見、健全な批判、建設的な反対意見を規制・抑制するためのものではありません。

人格否定・存在否定・差別的表現・強い罵倒・執拗な攻撃・脅迫・個人情報晒し示唆など、受け手の人の尊厳や安全感を大きく損なう可能性のある投稿に対して、ユーザーが「読む / 今は読まない」を選べるようにする補助ツールです。

## 基本コンセプト

「ことばうけみまもり｜Xことばに心のワンクッション」は、X（旧Twitter）で届く言葉のなかで、人格否定・存在否定・差別的表現・執拗な攻撃など、受け手の心に大きな負荷を与える可能性のある投稿に、そっとワンクッションを置くための補助ツールです。

異なる意見や健全な批判を規制することを目的とせず、ユーザーが大切にしている「つながり」と「対話」を守りながら、心の負担を軽くする選択肢を提供します。

## 最重要原則

本リポジトリを変更する場合は、必ず以下の原則を守ってください。

1. 投稿者を裁くのではなく、読む側を守る。
2. 検閲ではなく、読む前のワンクッションを提供する。
3. ユーザーの選択権を優先する。
4. MVPでは過検知を避け、高信頼性を優先する。
5. 判定処理は原則ローカルで行う。
6. 投稿本文や判定結果を外部サーバーへ送信しない。
7. ユーザーの精神状態を推定しない。
8. 法律判断、医療・心理診断を行わない。
9. 自動ブロック、自動通報、アカウント危険度判定を行わない。
10. UI文言は落ち着いた、非断定的な表現にする。

## MVPの対象範囲

MVPでは、高信頼性のある検知に限定してください。

### MVPでやること

- X上の表示済みテキストを対象にする。
- リプライ、通知、引用ポスト、検索結果、タイムライン上の投稿を候補にする。
- 以下のような高リスク表現を検知する。
  - 存在否定
  - 人格否定
  - 差別的表現・属性攻撃
    - 属性攻撃、人の属性(例: 職業・立場・地位・外見・出自・技能・年齢・性別・背景・資産・収入・国籍・趣味などの様々な属性)だけを見て、人を類型化して断定し、偏見や侮辱する攻撃のこと 
  - 強い罵倒・侮辱
- 高リスク投稿をぼかす。
- 読む前のワンクッションUIを表示する。
- ユーザーが以下を選べるようにする。
  - 内容を表示する
  - 今は見ない
- 固定ルールによる補助情報として、表現の強さの目安と検知された表現の傾向を表示できるようにする。
- 判定処理はローカルで行う。
- 投稿本文や判定結果を外部送信しない。

### MVPでやらないこと

MVPでは、以下を実装しないでください。

- 自動ブロック
- 自動通報
- 共有ブロックリスト
- アカウント危険度スコア
- 攻撃者リスト
- DM本文の解析
- 法律判断
- 医療・心理診断
- ユーザーの精神状態推定
- 投稿の自動削除
- 投稿の自動消去
- 健全な批判・異論・反対意見を規制する機能

## 倫理・安全方針

このプロジェクトは、意見を抑制するためのツールになってはいけません。

健全な批判、異なる意見、説明要求、根拠の確認、事実訂正は、原則として有害判定しないでください。

この拡張機能が対象にするのは、意見そのものではなく、受け手の人格・存在・属性・尊厳・安全感を直接攻撃するような表現です。

### 投稿者を断定的にラベル付けしない

以下のような表示文言は使わないでください。

- この人は加害者です
- このアカウントは危険です
- 攻撃者です
- 悪意があります
- 危険人物です

代わりに、可能性を示す表現を使ってください。

推奨する文言例:

- 心に負荷がかかる可能性のある表現を検知しました
- 人格否定に近い表現の可能性があります
- 存在否定に近い表現の可能性があります
- 属性への攻撃に近い表現の可能性があります
- 強い侮辱表現に近い内容の可能性があります

## プライバシー要件

プライバシーは最重要要件です。

### 必ず守ること

- 投稿本文の判定は原則ローカルで行う。
- 投稿本文を外部サーバーへ送信しない。
- 判定結果を外部サーバーへ送信しない。
- 閲覧履歴を収集しない。
- ユーザーIDや個人識別情報を収集しない。
- ユーザーの精神状態を推定・保存しない。
- 生の投稿本文を標準設定で永続保存しない。

### 保存が必要な場合

設定値の保存には `chrome.storage.local` を使用してください。

`chrome.storage.local` に保存してよいのは、ユーザーが明示的に選択した設定値のみです。現在のMVPでは、拡張機能の有効 / 無効を示す `enabled`、ワンクッションの表示されやすさを示す `cushionSensitivity`、表示言語を示す `uiLanguage` の3つに限定してください。

投稿本文、判定結果、閲覧履歴、ユーザーID、投稿URL、`score`、`matchedRules`、`categories`、`reasons`、`riskLevel`、`shouldCushion`、`guidance`、`strengthKey`、`tendencyKeys` は保存しないでください。これらを Chrome Storage、Local Storage、IndexedDB、DOM data 属性へ保存したり、外部送信したりしないでください。

`content.js` の本番動作は、ユーザーが明示的に `enabled=true` にした場合のみ有効にしてください。初期値は `enabled=false` とし、`enabled=false` では表示変更・ぼかし・ワンクッションUI・候補属性付与を行わないでください。

`cushionSensitivity` の初期値は `standard` とし、`low` / `standard` / `high` 以外の値は `standard` に正規化してください。ON/OFFと表示されやすさの変更は、現時点ではXページ再読み込み後に反映してください。

`uiLanguage` の初期値は `auto` とし、`auto` / `ja` / `en` 以外の値は `auto` に正規化してください。popup、options、X上のワンクッションUIは同じ `uiLanguage` を使用します。popup と options は変更した画面ですぐに切り替え、X上のワンクッションUIはXページ再読み込み後に切り替えてください。`auto` は Chrome UI 言語が日本語系なら `ja`、それ以外なら `en` に解決します。翻訳文言は `_locales` を正本とし、外部翻訳APIや外部通信を利用しません。

`resolvedLanguage` と `localeMessages` はページ実行中のメモリ上だけで扱い、Chrome Storage、Local Storage、IndexedDB、DOM data属性へ永続保存してはいけません。`overlay.js` はStorageを直接読まず、`content.js` がページ初期化時に準備したlocalizerを依存注入してください。localeは初期化時に1回だけ読み込み、投稿ごとに再読み込みしてはいけません。JavaScriptへ日英辞書を重複コピーせず、`_locales/ja/messages.json` と `_locales/en/messages.json` を翻訳文言の正本として維持してください。

X上の既存ワンクッションUIを `chrome.storage.onChanged` でリアルタイム再描画してはいけません。初期表示、ガイダンス、ボタン、「今は見ない」後の表示には同じlocalizerを使い、言語変更はページ再読み込みを反映境界としてください。外部翻訳API、外部CDN、外部localeファイルを利用してはいけません。

Manifest V3の `web_accessible_resources` は、X上のcontent scriptが選択言語のlocale JSONを読み込み、翻訳をJavaScriptへ二重コピーしないためにだけ利用します。公開対象は `_locales/ja/messages.json` と `_locales/en/messages.json`、対象オリジンは `https://x.com/*` と `https://twitter.com/*` だけに限定してください。`_locales/*`、拡張機能全体、不要なドメイン、`<all_urls>` へ安易に広げたり、不要な `host_permissions` を追加したりしてはいけません。

`web_accessible_resources` として公開したファイルは、指定した対象オリジン側からアクセス可能です。公開ファイルには固定UI文言だけを置き、センシティブ情報、秘密情報、ユーザー固有情報、投稿本文、判定結果、URL、Xユーザー情報、`score`、`matchedRules`、`categories`、`reasons`、`riskLevel`、`shouldCushion`、`guidance`、`strengthKey`、`tendencyKeys` を含めてはいけません。将来公開対象を増やす場合は、必要性と安全性を事前に確認してください。この設定は外部通信や外部翻訳APIのためのものではなく、投稿本文や判定結果を外部公開・外部送信するものではありません。

`chrome.storage.onChanged` によるリアルタイム反映を実装する場合は、既存DOM状態、ワンクッションUI、ぼかし、`data-kum-*` 属性を安全に復元・停止できる設計を先に確認してください。投稿本文や投稿DOMは削除しないでください。

## 判定方針

MVPでは、ローカルのルールベース + スコアリング方式を採用してください。

初期実装では、検知漏れをゼロにすることよりも、過検知を避けることを優先してください。

方針:

> 有害な投稿を一部見逃すことよりも、健全な批判や異論を有害扱いしてしまうことを避ける。

### 内部スコア

投稿本文に対して、内部的に 0〜100 のリスクスコアを算出してください。

| スコア | 内部判定 | MVPでの扱い |
|---:|---|---|
| 0〜29 | 低リスク | 何もしない |
| 30〜59 | 注意 | 何もしない |
| 60〜79 | 中リスク | 標準設定では表示変更しない |
| 80〜100 | 高リスク | ぼかし + ワンクッション表示 |

標準設定では、`score >= 80` の場合のみ `shouldCushion: true` にしてください。表示されやすさは `low: 100` / `standard: 80` / `high: 60` のしきい値へ対応付け、健全な批判・異論・反対意見を対象にする方針へ変更しないでください。

### 推奨する判定結果の形式

`risk-detector.js` は、以下のような形式の結果を返してください。

```js
{
  riskLevel: 'high',
  score: 92,
  shouldCushion: true,
  categories: [
    'existence_denial',
    'direct_attack'
  ],
  reasons: [
    '存在否定に近い表現の可能性があります'
  ],
  matchedRules: [
    'existence_denial.strong_phrase',
    'direct_attack.second_person'
  ]
}
```

`score` と `matchedRules` はユーザーに表示しないでください。  
これらは内部処理、デバッグ、テスト用です。

## 判定カテゴリ

カテゴリIDは、安定していてテストしやすい名前にしてください。

推奨カテゴリID:

- `existence_denial`
- `personality_attack`
- `discriminatory_attack`
- `severe_insult`
- `persistent_attack`
- `threat_or_harm`
- `doxxing_or_privacy_risk`
- `healthy_criticism`
- `quoted_or_contextual`

### MVPで重点的に扱うカテゴリ

MVPでは、主に以下を扱ってください。

- `existence_denial`
- `personality_attack`
- `discriminatory_attack`
- `severe_insult`

### 減点・除外したい文脈

以下に該当する場合は、スコアを下げるか、ワンクッション対象外にしてください。

- 健全な批判
- 異なる意見
- 説明を求める表現
- 根拠を求める表現
- 注意喚起
- 教育的な文脈
- 攻撃語の引用
- 被害経験の共有
- ニュース・相談文脈

原則としてワンクッション対象外にしたい例:

- その意見には反対です
- 根拠を示してほしいです
- その発言は不適切だと思います
- 「消えろ」と言われてつらかった
- 人に「存在価値がない」と言うのはよくない
- 誹謗中傷はやめよう

## UI/UX方針

UIは、落ち着いた、やさしい、非断定的な表現にしてください。

ワンクッションUIでは、以下を守ってください。

- 怖がらせる表現を避ける。
- 投稿者を責める表現を避ける。
- 法律・医療・心理診断のような表現を避ける。
- ユーザーが読むかどうか選べるようにする。
- 内容の表示と非表示を簡単に切り替えられるようにする。
- 元の投稿をDOM上から削除しない。
- 証拠保存の妨げにならないようにする。
- 「表現の強さの目安」と「検知された表現の傾向」は、利用者が本文を開く前に考えるための固定ルールによる補助情報として扱う。
- 生の `score`、`matchedRules`、`reasons` の内部詳細、`riskLevel`、`shouldCushion`、生の内部カテゴリIDをユーザーへ表示しない。
- 表現の強さは `somewhatStrong` / `strong` / `veryStrong` に対応する3段階だけにし、傾向は安全に抽象化した表示用カテゴリから最大2件に限定する。
- 傾向の表示優先順位は、危険度・深刻度・被害の大小・投稿者の攻撃性のランキングとして扱わない。
- ガイダンスはAIによる文章理解・危険性判断ではなく、投稿の危険性・事実関係・投稿者の人格や悪意を断定・評価するものではない。
- 感度設定はワンクッションの表示しきい値だけへ影響させ、同じ本文の `score` や表現の強さの意味を変えない。
- `overlay.js` の表示可能な guidance キーはホワイトリスト方式を維持し、未知のキーは表示しない。
- 「今は見ない」後は、利用者の判断を尊重して guidance を残さない。
- 強さごとに赤い警告色、危険アイコン、危険度ゲージ、プログレスバー、強い色分けを加えない。
- 引用する側の投稿と引用元投稿は独立した対象として扱い、ガイダンスや表示・解除状態を相互に伝播させない。

推奨するUI文言:

```text
読む前に、少しだけワンクッションを置きました

この投稿には、心に負荷がかかる可能性のある表現が含まれているかもしれません。

心に負荷がかかる可能性のある表現を検知しました

表現の強さの目安: 強め

検知された表現の傾向:
・人に向けた強い表現
・圧を感じる可能性のある表現

固定ルールによる補助的な目安です。

[内容を表示する] [今は見ない]
```

## 多言語対応方針

本拡張機能は、初期実装から多言語対応を前提にしてください。

Chrome拡張機能の i18n 仕組みに合わせて、UIに表示する固定文言、manifest上の名称・説明文、オプション画面の文言、ワンクッションUIの文言、判定理由文は、可能な限り `_locales` 配下の `messages.json` で管理してください。

popup と options の表示言語切り替えでは、既存の `getMessage()` を維持したうえで、拡張機能内に同梱された `_locales/ja/messages.json` または `_locales/en/messages.json` を安全に読み込んでください。JavaScriptへ日英辞書を重複コピーしてはいけません。

### 初期対応言語

初期対応言語は以下です。

- 日本語: `ja`
- 英語: `en`

MVPでは日本語を主言語とし、英語も同時に管理してください。

### 必ず守ること

- UIに表示する固定文言を、原則として JavaScript / HTML に直接書かない。
- ユーザー向け文言は `chrome.i18n.getMessage()` で取得できる形にする。
- `manifest.json` の `name` と `description` は `__MSG_xxx__` 形式にする。
- `manifest.json` には `default_locale` を設定する。
- `_locales/ja/messages.json` と `_locales/en/messages.json` のキーを揃える。
- 新しいユーザー向け文言を追加した場合は、必ず日本語・英語の両方を追加する。
- 判定ロジックの内部カテゴリIDは英語の安定IDを使い、表示文言は i18n 側で管理する。
- 安全・倫理・プライバシーに関する文言は、意味が変わらないよう慎重に翻訳する。

### 禁止事項

以下は避けてください。

- ユーザー向け固定文言を複数ファイルに直書きすること。
- 日本語だけを追加し、英語の `messages.json` を更新しないこと。
- 英語だけを追加し、日本語の `messages.json` を更新しないこと。
- 判定カテゴリIDをそのままユーザーに表示すること。
- `score` や `matchedRules` などの内部情報をユーザー向け文言として表示すること。
- 翻訳によって、投稿者を断定的に責める表現へ変えること。
- 翻訳によって、法律判断・医療判断・心理診断のように見える表現へ変えること。

### 推奨ディレクトリ構成

```text
_locales
├─ ja
│  └─ messages.json
└─ en
   └─ messages.json
```

### メッセージキー命名方針

メッセージキーは、英語の camelCase を使ってください。

例:

- `extensionName`
- `extensionDescription`
- `cushionTitle`
- `cushionBody`
- `buttonShowContent`
- `buttonHideForNow`
- `reasonExistenceDenial`
- `reasonPersonalityAttack`
- `reasonDiscriminatoryAttack`
- `reasonSevereInsult`
- `optionEnableExtension`
- `optionDetectionStrength`

### 実装方針

i18n取得用の小さなヘルパー関数を用意しても構いません。

例:

```js
function getMessage(key, substitutions) {
  if (typeof chrome !== 'undefined' && chrome.i18n?.getMessage) {
    return chrome.i18n.getMessage(key, substitutions) || key;
  }

  return key;
}
```

ただし、テスト環境では `chrome.i18n` が存在しない場合があるため、テストしやすいようにフォールバックやモックを考慮してください。

### テスト方針

多言語対応に関する実装を追加した場合は、以下を確認してください。

- `_locales/ja/messages.json` と `_locales/en/messages.json` のキーが揃っていること。
- UIに表示する固定文言が、コード内に不要に直書きされていないこと。
- `chrome.i18n.getMessage()` が利用できないテスト環境でも、安全にフォールバックできること。
- `uiLanguage=auto` / `ja` / `en` の解決と、popup / options の即時反映をテストすること。
- manifest の `name` と `description` が `__MSG_xxx__` 形式になっていること。
- `default_locale` が設定されていること。

### ドキュメント更新方針

ユーザー向け文言、manifest文言、オプション画面文言、ワンクッションUI文言、判定理由文を追加・変更した場合は、以下を更新してください。

- `_locales/ja/messages.json`
- `_locales/en/messages.json`
- 必要に応じて README.md
- 必要に応じて AGENTS.md

## 実装方針

### 想定ファイル構成

```text
Chrome Extension
├─ manifest.json
├─ content.js
├─ risk-detector.js
├─ i18n.js
├─ _locales
│  ├─ ja
│  │  └─ messages.json
│  └─ en
│     └─ messages.json
├─ overlay.js
├─ options.html
├─ options.js
└─ chrome.storage.local
```

### `risk-detector.js`

役割:

- テキストの正規化
- リスクルールの適用
- 加点ルールの適用
- 減点ルールの適用
- スコア計算
- 判定結果の返却

禁止事項:

- DOMへアクセスしない。
- UIを変更しない。
- ネットワーク通信をしない。
- 生の投稿本文を保存しない。

### `content.js`

役割:

- XのDOM変更を監視する。
- 判定候補となる投稿テキストを抽出する。
- `detectTextRisk` を呼び出す。
- `settings.cushionSensitivity` に応じたしきい値を `detectTextRisk` へ渡す。
- `riskResult` を `cushion-guidance.js` で安全な `strengthKey` / `tendencyKeys` に変換し、targetNode単位で一時的に `overlay.js` へ渡す。
- 生の判定結果、投稿本文、URL、Xユーザー情報を `overlay.js`、Storage、DOM data 属性へ渡さない。
- 同じDOMノードを重複処理しない。
- 本番動作は `settings.enabled === true` の場合のみ行う。
- `settings.enabled === false` ではDOM監視・通常判定・候補属性付与・ワンクッションUI・ぼかし表示を行わない。

### `overlay.js`

役割:

- ぼかし表示を適用する。
- ワンクッションUIを描画する。
- 表示許可された guidance キーだけを利用者向けに表示し、未知値・不正値を安全に無視する。
- 「内容を表示する」を処理する。
- 「今は見ない」を処理する。
- 「今は見ない」後は guidance を表示しない簡潔な状態にする。
- 必要に応じて表示状態を安全に復元する。

### `options.js`

役割:

- ユーザー設定を読み込む。
- 拡張機能の有効 / 無効設定、表示言語、表示されやすさ設定を保存する。
- 保存状態メッセージを表示する。
- 投稿本文や判定結果を扱わない。

### `settings.js`

役割:

- 初期設定を `enabled: false` にする。
- 初期の表示されやすさ設定を `cushionSensitivity: 'standard'` にする。
- 初期の表示言語設定を `uiLanguage: 'auto'` にする。
- `chrome.storage.local` にはユーザーが明示的に選んだ設定値だけを保存する。
- テスト環境などで `chrome.storage.local` が使えない場合は安全な初期値にフォールバックする。
- 投稿本文、判定結果、閲覧履歴、ユーザーID、投稿URL、`score`、`matchedRules` を保存しない。

## コーディング方針

シンプルで読みやすい JavaScript を優先してください。

推奨:

- 小さな純粋関数を増やす。
- 判定ルールはデータ駆動で管理する。
- UI文言を複数ファイルに散らばらせない。
- MVPでは不要な依存ライブラリを増やさない。
- 外部ネットワーク通信を追加しない。
- XのDOM変更に備えて、DOMセレクタは分離し、更新しやすくする。
- 安全性に関わる処理にはコメントを残す。

## 静的解析・フォーマット方針

本リポジトリでは、JavaScript / JSON の静的解析とフォーマットに Biome を使用します。

コード変更時は、以下を実行してから完了としてください。

```bash
npm run check
```

個別に確認する場合:

```bash
npm run lint
npm run format:check
npm test
```

フォーマット修正が必要な場合:

```bash
npm run format
```

CodexなどのAIコーディングエージェントは、コードを変更した場合、可能な限り `npm run check` が通る状態にしてください。

## テスト方針

テストでは、過検知を防ぐことを重視してください。

追加したいテスト:

- 高リスク表現がワンクッション対象になること。
- 健全な批判がワンクッション対象にならないこと。
- 引用された攻撃語がワンクッション対象にならないこと。
- 被害経験の共有がワンクッション対象にならないこと。
- 短い強攻撃表現がワンクッション対象になること。
- 二人称への直接攻撃が加点されること。
- 標準設定では `score >= 80` のときだけ `shouldCushion: true` になること。
- `low` / `standard` / `high` の設定が `100` / `80` / `60` のしきい値へ対応すること。
- 表示されやすさを変更しても、健全な批判・異論・反対意見を過剰に対象としないこと。

重要なテスト例:

```text
その意見には反対です
根拠を示してほしいです
「消えろ」と言われてつらかった
人に「存在価値がない」と言うのはよくない
お前なんか存在価値がない
消えろ
人として終わってる
```

## ドキュメント更新方針

挙動を変更した場合は、関連ドキュメントも更新してください。

README.md を更新すべき変更:

- MVP対象範囲の変更
- プライバシー方針の変更
- 判定カテゴリの変更
- スコアリング方針の変更
- UI/UX方針の変更
- 対象外項目の変更

AGENTS.md を更新すべき変更:

- 安全原則の変更
- 実装ルールの変更
- プライバシー要件の変更
- テスト方針の変更
- AIコーディングエージェント向け指示の変更

## 禁止する実装

プロジェクトオーナーが将来の設計変更として明示的に承認しない限り、以下を実装しないでください。

- 投稿本文をサーバーへ送信する機能
- 判定結果をサーバーへ送信する機能
- アカウント単位の危険度スコア
- 共有ブロックリスト
- 自動ブロック
- 自動通報
- ユーザー制御なしの自動完全非表示
- 精神状態の診断
- 法律判断
- 投稿者を攻撃者として公開ラベル付けする機能
- 生の投稿本文を標準設定で永続保存する機能
- DM本文の解析

## 現在のステータス

現在のフェーズ:

```text
Chrome拡張機能の最小構成を追加済み。MVPのDOM解析・ワンクッション適用は今後実装予定。
```

次の予定:

- XのDOM抽出方式の検証
- 投稿本文抽出とDOM監視の実装
- `risk-detector.js` と `overlay.js` の接続
- ワンクッションUIの実画面への適用
- Chrome拡張機能としての手動読み込み確認
