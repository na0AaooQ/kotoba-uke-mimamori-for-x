# AGENTS.md

このドキュメントは、本リポジトリで作業する Codex などの AI コーディングエージェント向けの開発方針です。

リポジトリ
https://github.com/na0AaooQ/kotoba-uke-mimamori-for-x

## プロジェクト概要

本リポジトリは、Google Chrome拡張機能「ことばうけみまもり｜Xことばに心のワンクッション」のソースコード管理リポジトリです。

この拡張機能は、X（旧Twitter）で届く言葉のなかで、受け手の人の心に大きな負荷を与える可能性のある投稿について、読む前のワンクッションを置くことを目的とします。

思想や信条、異なる意見、健全な批判、建設的な反対意見を規制・抑制するためのものではありません。

人格否定・存在否定・差別的表現・強い罵倒・執拗な攻撃・脅迫・個人情報晒し示唆など、受け手の人の尊厳や安全感を大きく損なう可能性のある投稿に対して、ユーザーが「読む / 今は読まない」を選べるようにする補助ツールです。

## 基本コンセプト

「ことばうけみまもり｜Xことばに心のワンクッション」は、X（旧Twitter）で届く言葉のなかで、人格否定・存在否定・差別的表現・執拗な攻撃など、受け手の心に大きな負荷を与える可能性のある投稿に、そっとワンクッションを置くための補助ツールです。

異なる意見や健全な批判を規制することを目的とせず、ユーザーが大切にしている「つながり」と「対話」を守りながら、心の負担を軽くする選択肢を提供します。

## 最重要原則

本リポジトリを変更する場合は、必ず以下の原則を守ってください。

1. 投稿者を裁くのではなく、読む側を守る。
2. 検閲ではなく、読む前のワンクッションを提供する。
3. ユーザーの選択権を優先する。
4. MVPでは過検知を避け、高信頼性を優先する。
5. 判定処理は原則ローカルで行う。
6. 投稿本文や判定結果を外部サーバーへ送信しない。
7. ユーザーの精神状態を推定しない。
8. 法律判断、医療・心理診断を行わない。
9. 自動ブロック、自動通報、アカウント危険度判定を行わない。
10. UI文言は落ち着いた、非断定的な表現にする。

## MVPの対象範囲

MVPでは、高信頼性のある検知に限定してください。

### MVPでやること

- X上の表示済みテキストを対象にする。
- リプライ、通知、引用ポスト、検索結果、タイムライン上の投稿を候補にする。
- 以下のような高リスク表現を検知する。
  - 存在否定
  - 人格否定
  - 差別的表現・属性攻撃
  - 強い罵倒・侮辱
- 高リスク投稿をぼかす。
- 読む前のワンクッションUIを表示する。
- ユーザーが以下を選べるようにする。
  - 内容を表示する
  - 今は見ない
- 判定理由を短く表示する。
- 判定処理はローカルで行う。
- 投稿本文や判定結果を外部送信しない。

### MVPでやらないこと

MVPでは、以下を実装しないでください。

- 自動ブロック
- 自動通報
- 共有ブロックリスト
- アカウント危険度スコア
- 攻撃者リスト
- DM本文の解析
- 法律判断
- 医療・心理診断
- ユーザーの精神状態推定
- 投稿の自動削除
- 投稿の自動消去
- 健全な批判・異論・反対意見を規制する機能

## 倫理・安全方針

このプロジェクトは、意見を抑制するためのツールになってはいけません。

健全な批判、異なる意見、説明要求、根拠の確認、事実訂正は、原則として有害判定しないでください。

この拡張機能が対象にするのは、意見そのものではなく、受け手の人格・存在・属性・尊厳・安全感を直接攻撃するような表現です。

### 投稿者を断定的にラベル付けしない

以下のような表示文言は使わないでください。

- この人は加害者です
- このアカウントは危険です
- 攻撃者です
- 悪意があります
- 危険人物です

代わりに、可能性を示す表現を使ってください。

推奨する文言例:

- 心に負荷がかかる可能性のある表現を検知しました
- 人格否定に近い表現の可能性があります
- 存在否定に近い表現の可能性があります
- 属性への攻撃に近い表現の可能性があります
- 強い侮辱表現に近い内容の可能性があります

## プライバシー要件

プライバシーは最重要要件です。

### 必ず守ること

- 投稿本文の判定は原則ローカルで行う。
- 投稿本文を外部サーバーへ送信しない。
- 判定結果を外部サーバーへ送信しない。
- 閲覧履歴を収集しない。
- ユーザーIDや個人識別情報を収集しない。
- ユーザーの精神状態を推定・保存しない。
- 生の投稿本文を標準設定で永続保存しない。

### 保存が必要な場合

設定値の保存には `chrome.storage.local` を使用してください。

保存してよい設定値の例:

- 拡張機能の有効 / 無効
- 検知強度
- 対象画面設定
- UI表示設定
- 将来的に追加するユーザーフィードバック設定

標準設定では、生の投稿本文を保存しないでください。

## 判定方針

MVPでは、ローカルのルールベース + スコアリング方式を採用してください。

初期実装では、検知漏れをゼロにすることよりも、過検知を避けることを優先してください。

方針:

> 有害な投稿を一部見逃すことよりも、健全な批判や異論を有害扱いしてしまうことを避ける。

### 内部スコア

投稿本文に対して、内部的に 0〜100 のリスクスコアを算出してください。

| スコア | 内部判定 | MVPでの扱い |
|---:|---|---|
| 0〜29 | 低リスク | 何もしない |
| 30〜59 | 注意 | 何もしない |
| 60〜79 | 中リスク | 原則として表示変更しない |
| 80〜100 | 高リスク | ぼかし + ワンクッション表示 |

MVPでは、`score >= 80` の場合のみ `shouldCushion: true` にしてください。

### 推奨する判定結果の形式

`risk-detector.js` は、以下のような形式の結果を返してください。

```js
{
  riskLevel: 'high',
  score: 92,
  shouldCushion: true,
  categories: [
    'existence_denial',
    'direct_attack'
  ],
  reasons: [
    '存在否定に近い表現の可能性があります'
  ],
  matchedRules: [
    'existence_denial.strong_phrase',
    'direct_attack.second_person'
  ]
}
```

`score` と `matchedRules` はユーザーに表示しないでください。  
これらは内部処理、デバッグ、テスト用です。

## 判定カテゴリ

カテゴリIDは、安定していてテストしやすい名前にしてください。

推奨カテゴリID:

- `existence_denial`
- `personality_attack`
- `discriminatory_attack`
- `severe_insult`
- `persistent_attack`
- `threat_or_harm`
- `doxxing_or_privacy_risk`
- `healthy_criticism`
- `quoted_or_contextual`

### MVPで重点的に扱うカテゴリ

MVPでは、主に以下を扱ってください。

- `existence_denial`
- `personality_attack`
- `discriminatory_attack`
- `severe_insult`

### 減点・除外したい文脈

以下に該当する場合は、スコアを下げるか、ワンクッション対象外にしてください。

- 健全な批判
- 異なる意見
- 説明を求める表現
- 根拠を求める表現
- 注意喚起
- 教育的な文脈
- 攻撃語の引用
- 被害経験の共有
- ニュース・相談文脈

原則としてワンクッション対象外にしたい例:

- その意見には反対です
- 根拠を示してほしいです
- その発言は不適切だと思います
- 「消えろ」と言われてつらかった
- 人に「存在価値がない」と言うのはよくない
- 誹謗中傷はやめよう

## UI/UX方針

UIは、落ち着いた、やさしい、非断定的な表現にしてください。

ワンクッションUIでは、以下を守ってください。

- 怖がらせる表現を避ける。
- 投稿者を責める表現を避ける。
- 法律・医療・心理診断のような表現を避ける。
- ユーザーが読むかどうか選べるようにする。
- 内容の表示と非表示を簡単に切り替えられるようにする。
- 元の投稿をDOM上から削除しない。
- 証拠保存の妨げにならないようにする。

推奨するUI文言:

```text
読む前に、少しだけワンクッションを置きました

この投稿には、心に負荷がかかる可能性のある表現が含まれているかもしれません。

理由: 人格否定に近い表現の可能性があります

[内容を表示する] [今は見ない]
```

## 実装方針

### 想定ファイル構成

```text
Chrome Extension
├─ manifest.json
├─ content.js
├─ risk-detector.js
├─ overlay.js
├─ options.html
├─ options.js
└─ chrome.storage.local
```

### `risk-detector.js`

役割:

- テキストの正規化
- リスクルールの適用
- 加点ルールの適用
- 減点ルールの適用
- スコア計算
- 判定結果の返却

禁止事項:

- DOMへアクセスしない。
- UIを変更しない。
- ネットワーク通信をしない。
- 生の投稿本文を保存しない。

### `content.js`

役割:

- XのDOM変更を監視する。
- 判定候補となる投稿テキストを抽出する。
- `detectTextRisk` を呼び出す。
- 高リスク判定結果を `overlay.js` に渡す。
- 同じDOMノードを重複処理しない。

### `overlay.js`

役割:

- ぼかし表示を適用する。
- ワンクッションUIを描画する。
- 判定理由を表示する。
- 「内容を表示する」を処理する。
- 「今は見ない」を処理する。
- 必要に応じて表示状態を安全に復元する。

### `options.js`

役割:

- ユーザー設定を管理する。
- 拡張機能の有効 / 無効を管理する。
- 検知強度を管理する。
- 対象画面設定を管理する。

## コーディング方針

シンプルで読みやすい JavaScript を優先してください。

推奨:

- 小さな純粋関数を増やす。
- 判定ルールはデータ駆動で管理する。
- UI文言を複数ファイルに散らばらせない。
- MVPでは不要な依存ライブラリを増やさない。
- 外部ネットワーク通信を追加しない。
- XのDOM変更に備えて、DOMセレクタは分離し、更新しやすくする。
- 安全性に関わる処理にはコメントを残す。

## テスト方針

テストでは、過検知を防ぐことを重視してください。

追加したいテスト:

- 高リスク表現がワンクッション対象になること。
- 健全な批判がワンクッション対象にならないこと。
- 引用された攻撃語がワンクッション対象にならないこと。
- 被害経験の共有がワンクッション対象にならないこと。
- 短い強攻撃表現がワンクッション対象になること。
- 二人称への直接攻撃が加点されること。
- `score >= 80` のときだけ `shouldCushion: true` になること。

重要なテスト例:

```text
その意見には反対です
根拠を示してほしいです
「消えろ」と言われてつらかった
人に「存在価値がない」と言うのはよくない
お前なんか存在価値がない
消えろ
人として終わってる
```

## ドキュメント更新方針

挙動を変更した場合は、関連ドキュメントも更新してください。

README.md を更新すべき変更:

- MVP対象範囲の変更
- プライバシー方針の変更
- 判定カテゴリの変更
- スコアリング方針の変更
- UI/UX方針の変更
- 対象外項目の変更

AGENTS.md を更新すべき変更:

- 安全原則の変更
- 実装ルールの変更
- プライバシー要件の変更
- テスト方針の変更
- AIコーディングエージェント向け指示の変更

## 禁止する実装

プロジェクトオーナーが将来の設計変更として明示的に承認しない限り、以下を実装しないでください。

- 投稿本文をサーバーへ送信する機能
- 判定結果をサーバーへ送信する機能
- アカウント単位の危険度スコア
- 共有ブロックリスト
- 自動ブロック
- 自動通報
- ユーザー制御なしの自動完全非表示
- 精神状態の診断
- 法律判断
- 投稿者を攻撃者として公開ラベル付けする機能
- 生の投稿本文を標準設定で永続保存する機能
- DM本文の解析

## 現在のステータス

現在のフェーズ:

```text
設計・MVP開発準備中
```

次の予定:

- `risk-detector.js` の初期実装
- 判定ロジックの単体テスト追加
- Chrome拡張機能の最小構成作成
- ワンクッションUIの試作
- XのDOM抽出方式の検証

